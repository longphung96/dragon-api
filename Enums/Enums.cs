namespace DragonAPI.Enums
{
    public enum SaleStatus
    {
        NotSale,
        Sale,
        InWorkplace
    }
    public enum StatusVerify
    {
        Processing,
        Accepted,
        Rejected,
        Failed
    }
    public enum TransactionType
    {
        Deposit,
        WithDraw
    }
    public enum Status
    {
        Processing,
        Success,
        Failed,
        Revert
    }
}
